/// colors File for all application
import 'package:flutter/material.dart';

/// This class contain all colors in Application so when you need to call a color should be select from here
class AppColors {
  /// This color return blue color
  static const Color blueColor = Color(0xFF18308A);

  /// This color return blue color
  static const Color lightBlueColor = Color(0xFFE9EDFF);

  /// This color return transparent color
  static const Color transparentColor = Colors.transparent;

  /// This color return black color and the hex code for it is [0xFF000000]
  static const Color blackColor = Color(0xFF000000);

  /// This color return white color and the hex code for it is [0xFFFFFFFF]
  static const Color whiteColor = Color(0xFFFFFFFF);

  /// This color return grey color
  static const Color greyColor = Color(0xFFF4F4F4);

  /// This color return green color
  static const Color greenColor = Color(0xFF93C98C);

  /// Priority Colors
  static const Color lowPriorityColor = Color(0xFF9E9AFF);
  static const Color mediunPriorityColor = Color(0xFFFFC59A);
  static const Color highPriorityColor = Color(0xFFFF9A9A);
}
